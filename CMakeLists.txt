PROJECT(video_streamer_webrtc)
cmake_minimum_required(VERSION 2.6)

SET (CMAKE_MODULE_PATH "${CMAKE_CURRENT_SOURCE_DIR}/.orogen/config")
INCLUDE(video_streamer_webrtcBase)

# FIND_PACKAGE(KDL)
# FIND_PACKAGE(OCL)

